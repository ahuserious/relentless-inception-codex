# EXPERIMENTAL OPT-IN EXAMPLE — merge into user-level ~/.codex/config.toml.
#
# OpenRouter's Responses API is currently beta and stateless-only. Codex 0.145
# custom providers advertise namespace tools, so the endpoint must support those
# requests as well as streamed function-call continuation. A successful text
# response does not prove either capability, and Chat Completions is no longer a
# Codex wire fallback. Keep OpenRouter models as external MCP panelists unless the
# complete native tool loop passes on the installed versions.

[model_providers.openrouter_ri]
name = "OpenRouter Responses (Relentless Inception)"
base_url = "https://openrouter.ai/api/v1"
env_key = "OPENROUTER_API_KEY"
env_key_instructions = "Set OPENROUTER_API_KEY in the environment that launches Codex."
wire_api = "responses"
request_max_retries = 2
stream_max_retries = 2
stream_idle_timeout_ms = 3600000
supports_websockets = false

# Example selection for a custom agent configuration layer:
# model_provider = "openrouter_ri"
# model = "x-ai/grok-4.5"
# model_reasoning_effort = "high"
